<h1>Quiz</h1>
<div>
  <strong> Name: </strong>
<%= @quiz.name %>
</div><br>


<%= form_for @trait, :url => { :action => "createtrait" } do |f| %>
<% @questions.each do |q| %>
  <%=f.label :question, q.content %> 
    <% q.answers.each do |a| %>
    <%= f.radio_button :content, "wtf" %><%= a.content %><br><br>
    <% end %>
    <% end %>
     <div class="actions">
        <%= f.submit "Submit", :name => 'quiz_results' %>
      </div>


    <div class="actions">
        <%= f.submit "Submit2" %>
      </div>
    <% end %>

<% if params[:quiz_results] %>
<div id = "quizresults">
  <h2>It works</h2>
</div>
<% end %>
 

